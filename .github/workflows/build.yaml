name: Build and upload container images
on:
    workflow_dispatch:
    push:
      branches:
        - 'main'
      tags:
        - '*'
    pull_request:
      branches:
        - 'main'

jobs:
  build-and-upload:
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - file: debian9
            image: debian9
            platforms: linux/amd64,linux/arm64
          - file: debian10
            image: debian10
            platforms: linux/amd64,linux/arm64
          - file: debian11
            image: debian11
            platforms: linux/amd64,linux/arm64
          - file: debian11
            image: debian11-clang22
            build_args: "CLANG_VERSION=22"
            platforms: linux/amd64,linux/arm64
    runs-on: ubuntu-latest
    name: Build and upload ${{ matrix.image }} image
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-${{ matrix.image }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.image }}-
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Create Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}/${{ matrix.image }}
          tags: |
            # set latest tag for default branch
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=pr
            type=sha
      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: ${{ matrix.platforms }}
          push: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          file: ${{ matrix.file }}.containerfile
          build-args: ${{ matrix.build_args }}
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache
